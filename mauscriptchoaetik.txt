game.Players.PlayerAdded:Connect(function(player)
	local stats = Instance.new("Folder")
	stats.Name = "leaderstats"
	stats.Parent = player

	local Money = Instance.new("IntValue")
	Money.Name = "Money"
	Money.Value = 0
	Money.Parent = stats
end)local Animals = workspace:WaitForChild("Animals")

local function MakeSick(animal)
	local humanoid = animal:FindFirstChild("Humanoid")
	if humanoid then
		humanoid.Health = humanoid.MaxHealth * 0.5
	end
end

for _, a in pairs(Animals:GetChildren()) do
	MakeSick(a)
end

Animals.ChildAdded:Connect(function(a)
	task.wait(1)
	MakeSick(a)
end)local healAmount = 30
local reward = 10

script.Parent.Touched:Connect(function(hit)
	local model = hit.Parent
	local humanoid = model:FindFirstChild("Humanoid")

	if humanoid then
		humanoid.Health = math.min(humanoid.Health + healAmount, humanoid.MaxHealth)

		local player = game.Players:GetPlayerFromCharacter(hit.Parent)
		if player then
			player.leaderstats.Money.Value += reward
		end
	end
end)