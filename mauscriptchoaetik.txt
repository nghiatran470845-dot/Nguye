local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 40)
Title.Text = "nguyen me hack"
Title.TextScaled = true
Title.Parent = Framelocal Players = game:GetService("Players")

Players.PlayerAdded:Connect(function(player)
	player.CharacterAdded:Connect(function(character)
		local humanoid = character:WaitForChild("Humanoid")

		while humanoid.Parent do
			humanoid.Health = humanoid.MaxHealth
			task.wait(0.1)
		end
	end)
end)local Frame = script.Parent -- Cửa sổ thông báo

local function ShowAlert()
	Frame.Visible = true
	task.wait(5) -- Hiển thị 5 giây
	Frame.Visible = false
end

game.ReplicatedStorage.AnimalNeedsHelp.OnClientEvent:Connect(function()
	ShowAlert()
end)local function AcceptAnimal(animal)
	if animal:GetAttribute("Condition") == "Normal" then
		animal:SetAttribute("Accepted", true)
		print("Đã tự động nhận:", animal.Name)
	end
end

workspace.Animals.ChildAdded:Connect(function(animal)
	AcceptAnimal(animal)
end)

for _, animal in ipairs(workspace.Animals:GetChildren()) do
	AcceptAnimal(animal)
end